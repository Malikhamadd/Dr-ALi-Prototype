{
  "name": "dr-ali-prototype",
  "private": true,
  "type": "module",
  "scripts": {
    "build:firebase": "node scripts/build-firebase.js",
    "build": "npm run build:firebase",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "firebase": "^12.7.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
